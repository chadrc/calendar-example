import React from 'react';

/**
 * Main component for app.
 * @param props
 * @constructor
 */

const Main = (props) => (
    <section>
        <h2>Main Component</h2>
    </section>
);

export default Main;